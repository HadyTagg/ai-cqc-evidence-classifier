# =========================================
# App Runtime
# =========================================
streamlit>=1.36       # Web app framework for interactive dashboards and tools
pandas>=2.2           # Data analysis library for tabular data, CSVs, spreadsheets
PyYAML>=6.0           # Read/write YAML config and taxonomy files

# =========================================
# Document Parsing
# =========================================
python-docx>=1.1      # Read/write Microsoft Word (.docx) documents
openpyxl>=3.1         # Read/write Excel (.xlsx/.xlsm) files; used by pandas
xlrd>=2.0.1           # Read legacy Excel (.xls) files; used by pandas
beautifulsoup4>=4.12  # Parse and navigate HTML/XML documents
lxml>=5.2             # High-performance HTML/XML parser (used by BeautifulSoup)
chardet>=5.2          # Detect file character encoding (text, HTML, etc.)
requests>=2.32        # HTTP client for downloading documents/web pages

# =========================================
# Imaging / Previews
# =========================================
Pillow>=10.3          # Image manipulation and creation (PIL fork)
pdf2image>=1.17.0     # Convert PDF pages to images (requires poppler-utils on Linux)
pillow-heif>=0.16.0   # HEIC/HEIF image support (common in iOS photos)

# =========================================
# Email Parsing
# =========================================
extract-msg>=0.48.6   # Extract content/attachments from Outlook .msg email files


# Core converters used by the app
sudo apt install -y libreoffice poppler-utils

# Fonts (for crisp email previews + monospace options)
sudo apt install -y fonts-dejavu-core fonts-liberation fonts-noto-core fonts-noto-mono

# Optional: HEIC support (only if pillow-heif complains about missing libheif)
sudo apt install -y libheif1